// JSON -> Java Script Object Notation
// we will make anothe file with .json extension
// it will run in JSON server

// it will install with the help of cmd
// npm install -g json-server is the command
// to check that it install or not we will give commang=d -> json-server --version

// to run the json fill we will give command to the terminal
// command -> json-server --watch data.json

// 